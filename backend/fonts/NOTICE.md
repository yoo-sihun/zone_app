`NanumSquareR.ttf` — Naver Nanum Square Regular, licensed under the
SIL Open Font License 1.1 (https://scripts.sil.org/OFL). Free to embed
and redistribute. Used by [reports.py](../reports.py) to render Korean
text in generated PDFs (reportlab's non-embedded CID fonts don't render
without a matching Hangul font on the viewer's machine, so we embed
this one directly into the PDF instead).
